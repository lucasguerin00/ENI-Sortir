<header class="bg-white shadow-md">
    <div class="container mx-auto py-3 flex items-center justify-between">
        <div class="flex items-center">
            <a href="{{ path('app_sortie_list') }}" class="text-xl font-bold text-gray-800">ENI-Sortir.com</a>
        </div>
        <div>
            {% if app.user %}
                <span class="text-gray-700 mr-4">
                    Bonjour, <strong>{{ app.user.prenom }} {{ app.user.nom }}</strong>
                </span>
                <a href="{{ path('profil', {'id': app.user.id}) }}" class="px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded-lg transition">Profil</a>
                <a href="{{ path('app_logout') }}" class="px-4 py-2 bg-red-500 hover:bg-red-600 text-white rounded-lg transition">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="px-4 py-2 bg-indigo-500 hover:bg-indigo-600 text-white rounded-lg transition">Connexion</a>
            {% endif %}
        </div>
    </div>
</header>
