{% extends 'base.html.twig' %}

{% block title %}Villes{% endblock %}

{% block body %}
    <div class="max-w-7xl mx-auto mt-10 p-6 bg-gray-50 rounded-lg shadow">

        <div class="flex justify-between items-center mb-6">
            <h1 class="text-3xl font-bold text-gray-800">Gérer les villes</h1>
            <a href="/ville/add"
               class="px-5 py-2.5 bg-green-700 text-white rounded-lg hover:bg-green-800 transition font-medium text-sm">
                Ajouter
            </a>
        </div>

        {# Flash messages #}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="mb-4 px-4 py-3 rounded
                {% if label == 'success' %}bg-green-200 text-green-800
                {% elseif label == 'error' %}bg-red-200 text-red-800
                {% else %}bg-gray-200 text-gray-800{% endif %}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        {% if villes is empty %}
            <p class="text-gray-600 italic">Aucune ville n'a été créée pour le moment.</p>
        {% else %}
            <div class="overflow-x-auto shadow-lg rounded-lg mt-6">
                <table class="min-w-full divide-y divide-gray-200 bg-white">
                    <thead class="bg-gray-100 text-gray-700 uppercase text-sm">
                    <tr>
                        <th class="px-6 py-3 text-left">Nom</th>
                        <th class="px-6 py-3 text-left">Code Postal</th>
                        <th class="px-6 py-3 text-left">Action</th>
                    </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200 text-sm text-gray-800">
                    {% for ville in villes %}
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4 font-medium">{{ ville.nom }}</td>
                            <td class="px-6 py-4">{{ ville.codePostal }}</td>
                            <td class="px-6 py-4 flex flex-col sm:flex-row gap-2">
                                <a href="/ville/del/{{ ville.id }}"
                                   class="px-3 py-1 bg-red-600 text-white rounded hover:bg-red-700 text-xs text-center transition">
                                    Supprimer
                                </a>
                                <a href="/ville/edit/{{ ville.id }}"
                                   class="px-3 py-1 bg-yellow-400 text-gray-800 rounded hover:bg-yellow-500 text-xs text-center transition">
                                    Modifier
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}
    </div>
{% endblock %}
